services:
  web:
    build:
      context: .
      dockerfile: .devcontainer/Dockerfile.dev
    command: sleep infinity
    working_dir: /workspace
    volumes:
      - ..:/workspace:cached
    ports:
      - "8000:8000"
